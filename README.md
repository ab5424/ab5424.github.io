# ab5424.github.io

This is the repository for [my personal website](ab5424.github.io).

## Development

- Install dependencies: `bundle install`
- Run locally: `bundle exec jekyll serve`

## Deployment

The site is deployed with GitHub Actions using [.github/workflows/pages.yml](.github/workflows/pages.yml).

In the repository settings, set Pages source to **GitHub Actions**.
